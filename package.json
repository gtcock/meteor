{
  "name": "download-execute-app",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "deploy": "DEPLOY_HOSTNAME=ap-southeast-2.galaxy.meteor.com meteor deploy lostme.au.meteorapp.com --settings settings.json --owner doomlife"
  },
  "dependencies": {
    "@babel/runtime": "^7.17.9",
    "meteor-node-stubs": "^1.2.1"
  }
} 
