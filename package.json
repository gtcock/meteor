{
  "name": "download-execute-app",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "deploy": "meteor deploy lostme.au.meteorapp.com --owner doomlife --settings settings.json"
  },
  "dependencies": {
    "@babel/runtime": "^7.17.9",
    "meteor-node-stubs": "^1.2.1"
  }
} 
