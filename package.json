{
  "name": "download-execute-app",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "build": "meteor build ../build --directory",
    "deploy": "meteor deploy lostme.au.meteorapp.com --owner doomlife"
  },
  "dependencies": {
    "@babel/runtime": "^7.17.9",
    "meteor-node-stubs": "^1.2.1"
  }
} 
